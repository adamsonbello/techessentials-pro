// Ajouter dans les fonctions
function generateCustomTemplate($articles, $subject) {
    // Coller le HTML du template tiers ici
    // Adapter avec vos variables $articles
    return $content;
}

// Ajouter dans le switch
case 'custom':
    return generateCustomTemplate($articles, $subject);